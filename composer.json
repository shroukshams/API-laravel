{
    "$schema": "https://getcomposer.org/schema.json",
    "name": "admin9/api-laravel",
    "type": "project",
    "description": "Laravel 13 backend for the Admin9 middle/back-office API.",
    "keywords": ["admin9", "laravel", "api", "back-office"],
    "license": "MIT",
    "require": {
        "php": "^8.3",
        "laravel/framework": "^13.8",
        "laravel/tinker": "^3.0",
        "mitoop/laravel-api-response": "^7.6",
        "mitoop/laravel-efficient-form-request": "^1.1",
        "mitoop/laravel-query-builder": "^2.11",
        "mitoop/laravel-query-logger": "^3.1",
        "php-open-source-saver/jwt-auth": "^2.9",
        "spatie/laravel-activitylog": "^4.12",
        "spatie/laravel-permission": "^8.0"
    },
    "require-dev": {
        "admin9/laravel-scramble-extensions": "^0.3",
        "barryvdh/laravel-ide-helper": "^3.7",
        "dedoc/scramble": "^0.13.30",
        "fakerphp/faker": "^1.23",
        "laravel/boost": "^2.4",
        "laravel/pail": "^1.2.5",
        "laravel/pao": "^1.0.6",
        "laravel/pint": "^1.27",
        "mockery/mockery": "^1.6",
        "nunomaduro/collision": "^8.6",
        "phpunit/phpunit": "^12.5.12"
    },
    "autoload": {
        "psr-4": {
            "App\\": "app/",
            "Database\\Factories\\": "database/factories/",
            "Database\\Seeders\\": "database/seeders/"
        }
    },
    "autoload-dev": {
        "psr-4": {
            "Tests\\": "tests/"
        }
    },
    "scripts": {
        "setup": [
            "composer install",
            "@php -r \"file_exists('.env') || copy('.env.example', '.env');\"",
            "@php artisan key:generate",
            "@php artisan jwt:secret --always-no",
            "@php -r \"file_exists('database/database.sqlite') || touch('database/database.sqlite');\"",
            "@php artisan migrate --force",
            "@php artisan storage:link --force",
            "npm ci --ignore-scripts",
            "npm run build"
        ],
        "dev": [
            "Composer\\Config::disableProcessTimeout",
            "npx concurrently -c \"#93c5fd,#c4b5fd,#fb7185,#fdba74,#86efac\" \"php artisan serve\" \"php artisan queue:listen --tries=1 --timeout=0\" \"php artisan schedule:work\" \"php artisan pail --timeout=0\" \"npm run dev\" --names=server,queue,schedule,logs,vite --kill-others"
        ],
        "test": [
            "@php artisan config:clear --ansi @no_additional_args",
            "@php artisan test --compact"
        ],
        "test:mysql-concurrency": [
            "@php artisan config:clear --ansi @no_additional_args",
            "@php ./vendor/bin/phpunit --configuration=phpunit.mysql.xml"
        ],
        "check": [
            "@docs:api:check",
            "@php artisan test --compact",
            "./vendor/bin/pint --test"
        ],
        "docs:api": [
            "@php -r \"is_dir('docs') || mkdir('docs', 0777, true);\"",
            "APP_URL=http://localhost php artisan scramble:export --env=local --no-interaction"
        ],
        "docs:api:check": [
            "@docs:api",
            "git diff --exit-code -- docs/api.json"
        ],
        "post-autoload-dump": [
            "Illuminate\\Foundation\\ComposerScripts::postAutoloadDump",
            "@php artisan package:discover --ansi"
        ],
        "post-update-cmd": [
            "@php artisan vendor:publish --tag=laravel-assets --ansi --force"
        ],
        "post-root-package-install": [
            "@php -r \"file_exists('.env') || copy('.env.example', '.env');\""
        ],
        "post-create-project-cmd": [
            "@php artisan key:generate --ansi",
            "@php -r \"file_exists('database/database.sqlite') || touch('database/database.sqlite');\"",
            "@php artisan migrate --graceful --ansi"
        ],
        "pre-package-uninstall": [
            "Illuminate\\Foundation\\ComposerScripts::prePackageUninstall"
        ],
        "pint": [
            "./vendor/bin/pint --parallel"
        ],
        "ide-helper": [
            "@php artisan ide-helper:generate",
            "@php artisan ide-helper:meta",
            "@php artisan ide-helper:models --nowrite",
            "./vendor/bin/pint --quiet --dirty --parallel --preset=laravel"
        ]
    },
    "extra": {
        "laravel": {
            "dont-discover": []
        }
    },
    "config": {
        "optimize-autoloader": true,
        "preferred-install": "dist",
        "sort-packages": true,
        "allow-plugins": {
            "pestphp/pest-plugin": true,
            "php-http/discovery": true
        }
    },
    "minimum-stability": "stable",
    "prefer-stable": true
}
